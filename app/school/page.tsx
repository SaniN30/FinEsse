import { TierPlaceholder } from "@/components/TierPlaceholder";

export default function SchoolPage() {
  return (
    <TierPlaceholder
      tier="school"
      title="School"
      description="Pocket money planning, saving habits, and the first real budget — the School curriculum lands in a later phase."
    />
  );
}
