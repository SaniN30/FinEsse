import { TierPlaceholder } from "@/components/TierPlaceholder";

export default function CollegePage() {
  return (
    <TierPlaceholder
      tier="college"
      title="College"
      description="Student budgets, part-time income, and first credit decisions — the College curriculum lands in a later phase."
    />
  );
}
