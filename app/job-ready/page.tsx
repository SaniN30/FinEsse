import { TierPlaceholder } from "@/components/TierPlaceholder";

export default function JobReadyPage() {
  return (
    <TierPlaceholder
      tier="jobready"
      title="Job-Ready"
      description="Payslips, investing fundamentals, and long-term planning — the Job-Ready curriculum lands in a later phase."
    />
  );
}
